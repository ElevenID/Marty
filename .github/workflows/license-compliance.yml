# SPDX-License-Identifier: AGPL-3.0-only
name: License Compliance

on:
  workflow_dispatch:
  schedule:
    - cron: '20 6 * * 1'  # Weekly Monday, staggered across repositories.
  pull_request:
    branches: [main]
    paths:
      - 'pyproject.toml'
      - 'LICENSE'
      - 'README.md'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

env:
  MARTY_CORE_REVISION: 359df1d20f152e21783fe43078d753fa1b494ce4

jobs:
  build-native-wheels:
    name: Build pinned native wheels
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
      - name: Restore exact pinned native wheels
        id: native-wheel-cache
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: native-wheels
          key: native-wheels-v1-${{ runner.os }}-${{ runner.arch }}-py311-rust1971-maturin1141-${{ env.MARTY_CORE_REVISION }}-${{ hashFiles('scripts/build-native-wheels.sh') }}
      - if: ${{ steps.native-wheel-cache.outputs.cache-hit != 'true' }}
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          repository: ElevenID/marty-core
          ref: ${{ env.MARTY_CORE_REVISION }}
          path: marty-core
      - if: ${{ steps.native-wheel-cache.outputs.cache-hit != 'true' }}
        uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"
      - if: ${{ steps.native-wheel-cache.outputs.cache-hit != 'true' }}
        uses: dtolnay/rust-toolchain@4cda84d5c5c54efe2404f9d843567869ab1699d4
        with:
          toolchain: 1.97.1
      - name: Install native transport build dependencies
        if: ${{ steps.native-wheel-cache.outputs.cache-hit != 'true' }}
        run: sudo apt-get update && sudo apt-get install --yes libpcsclite-dev libdbus-1-dev pkg-config
      - name: Build exact pinned native wheels
        if: ${{ steps.native-wheel-cache.outputs.cache-hit != 'true' }}
        run: bash scripts/build-native-wheels.sh
      - name: Require exact pinned native wheel set
        shell: bash
        run: |
          test "$(find native-wheels -maxdepth 1 -name 'marty_rs-*.whl' | wc -l)" -eq 1
          test "$(find native-wheels -maxdepth 1 -name 'marty_verification_py-*.whl' | wc -l)" -eq 1
          test "$(find native-wheels -maxdepth 1 -name 'marty_iso18013-*.whl' | wc -l)" -eq 1
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: marty-native-linux
          path: native-wheels/*.whl
          if-no-files-found: error
          retention-days: 2

  metadata-consistency:
    name: License Metadata Consistency
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.11"

      - name: Check license metadata consistency
        run: |
          python - <<'EOF'
          import sys
          try:
              import tomllib
          except ImportError:
              import tomli as tomllib

          fail = False

          with open("pyproject.toml", "rb") as f:
              data = tomllib.load(f)

          proj = data.get("project", {})
          license_val = proj.get("license", {})

          # Support both {text = "..."} and {file = "..."} forms
          if isinstance(license_val, dict):
              license_text = license_val.get("text", license_val.get("file", ""))
          else:
              license_text = str(license_val)

          if "AGPL-3.0" not in license_text:
              print(f"FAIL: pyproject.toml license = {license_text!r}, expected AGPL-3.0-only")
              fail = True
          else:
              print(f"OK: pyproject.toml license = {license_text!r}")

          import os
          if not os.path.exists("LICENSE"):
              print("FAIL: LICENSE file is missing")
              fail = True
          else:
              print("OK: LICENSE present")

          with open("README.md") as f:
              readme = f.read()
          if "## License" not in readme:
              print("FAIL: README.md is missing a ## License section")
              fail = True
          else:
              print("OK: README.md has ## License section")

          sys.exit(1 if fail else 0)
          EOF

  dependency-license-inventory:
    name: Dependency License Inventory
    needs: build-native-wheels
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          version: "latest"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Install smart-card build dependencies
        run: sudo apt-get update && sudo apt-get install --yes libpcsclite-dev

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: marty-native-linux
          path: native-wheels

      - name: Install project dependencies
        run: uv sync --all-extras --find-links native-wheels

      - name: Generate dependency license inventory
        run: |
          uv pip install pip-licenses
          uv run pip-licenses --format=json > dependency-licenses.json

      - name: Upload license inventory
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: dependency-licenses
          path: dependency-licenses.json

  sbom:
    name: Generate SBOM
    needs: build-native-wheels
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1

      - uses: astral-sh/setup-uv@c771a70e6277c0a99b617c7a806ffedaca235ff9
        with:
          version: "latest"

      - name: Set up Python 3.11
        run: uv python install 3.11

      - name: Install smart-card build dependencies
        run: sudo apt-get update && sudo apt-get install --yes libpcsclite-dev

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c
        with:
          name: marty-native-linux
          path: native-wheels

      - name: Install project dependencies
        run: uv sync --all-extras --find-links native-wheels

      - name: Install cyclonedx-bom
        run: uv pip install cyclonedx-bom

      - name: Generate CycloneDX SBOM
        run: uv run cyclonedx-py environment "$(uv python find)" --of json --output-file sbom.cdx.json

      - name: Upload SBOM artifact
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: sbom
          path: sbom.cdx.json
